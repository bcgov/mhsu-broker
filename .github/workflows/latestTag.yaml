name: Tag Latest

on:
  release:
    types: [published]

jobs:
  tag:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: |
          git checkout ${GITHUB_REF#refs/*/}
          git tag -d latest
          git tag latest
          git push origin :latest
          git push origin latest
